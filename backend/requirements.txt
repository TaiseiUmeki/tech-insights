# FastAPI and ASGI server
fastapi==0.115.4
uvicorn[standard]==0.27.0
pydantic==2.10.2
pydantic-settings==2.6.1

# Database
sqlalchemy==2.0.25
alembic==1.13.1
psycopg2-binary==2.9.9
pgvector==0.3.6

# AI / ML
# sentence-transformers が最新 torch を解決するとCUDA依存が入るため、CPU向けwheelが提供されている版に固定
torch==2.3.1
sentence-transformers==3.3.1

# Environment
python-dotenv==1.0.1

# ---- Optional: docker-compose.yml で対応サービスを有効化したらコメントを外す ----
# AWS SDK (S3 via MinIO, SQS via ElasticMQ in local; AWS本物 in 本番)
# boto3==1.35.92
# Redis (キャッシュ / セッション / レート制限 / 非同期タスクのブローカー)
# redis[hiredis]==5.2.1

# Code Quality
ruff==0.8.4

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
httpx==0.25.2
faker==20.1.0
